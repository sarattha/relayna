# Relayna v1.4.27 Studio Production Freeze And Operator Guide

This ExecPlan is a living document maintained in accordance with
`/Users/jobz/Works/relayna/PLANS.md`. Progress, discoveries, decisions, and
outcomes are updated while the work proceeds.

## Purpose / Big Picture

Release `v1.4.27` as the new strict production perimeter for the Relayna SDK,
Studio backend, and Studio frontend. The release documentation must let an
operator register and configure a service without reading source code, and must
explain every visible Studio navigation item, action, filter, and configuration
area with screenshots captured from the production Docker frontend.

## Progress

- [x] (2026-07-12) Selected `v1.4.27` as the next patch version and reviewed
  freeze, compatibility, verification, browser, publishing, and review-comment
  guidance.
- [x] (2026-07-12) Established the compatibility boundary: snapshot the
  already-approved branch behavior; preserve released routes, response shapes,
  Redis/RabbitMQ formats, and persisted documents.
- [x] (2026-07-12) Bumped SDK, Studio backend, Studio frontend, dependency floors, locks,
  release instructions, freeze manifests, freeze assertions, contributor
  policy, and freeze skill to `v1.4.27`.
- [x] (2026-07-12) Added the `v1.4.27` changelog entry.
- [x] (2026-07-12) Captured production Studio screenshots for overview, registration,
  observability configuration, search, incidents, and service/task operations.
- [x] (2026-07-12) Expanded the Studio frontend guide with step-by-step registration and a
  complete control/configuration reference.
- [x] (2026-07-12) Ran full SDK/backend verification, frontend and backend coverage gates,
  strict documentation build, production Docker builds, and live stack checks.
- [x] (2026-07-12) Verified the rebuilt Docker stack with Redis, RabbitMQ, direct backend
  discovery, frontend-proxied discovery, and installed SDK/backend versions.
- [ ] Commit, push, update PR #107, mark it ready, and monitor the first review
  cycle.

## Surprises & Discoveries

- The repository manifests already use `v1.4.26`, while `AGENTS.md` and the
  freeze skill still describe `v1.4.21` as the policy boundary. This release
  intentionally reconciles both policy and machine-readable manifests at
  `v1.4.27`.

## Decision Log

- Decision: Use `v1.4.27` as the next version.
  Rationale: `v1.4.26` is the latest tag and the requested work is a compatible
  patch release.
  Date/Author: 2026-07-12 / Codex.
- Decision: Update freeze manifest versions without changing their recorded
  symbol, signature, route, or page inventories except for behavior already
  approved and present on this branch.
  Rationale: A new freeze should record the reviewed perimeter, not mask drift
  or create a compatibility shim.
  Date/Author: 2026-07-12 / Codex.
- Decision: Store screenshots under `docs/assets/images/studio/` and reference
  them from `docs/studio-frontend.md` using relative Markdown links.
  Rationale: MkDocs copies assets predictably and the images remain reviewable
  alongside the guide.
  Date/Author: 2026-07-12 / Codex.

## Outcomes & Retrospective

The release candidate now consistently identifies as `v1.4.27`, and all three
machine-readable freeze surfaces enforce that perimeter. The operator guide is
backed by nine screenshots from the production frontend and documents service
registration plus every visible page-level control and configuration group.

Verification completed with 416/416 SDK tests, 241/241 Studio backend tests,
and 91/91 Studio frontend tests. Backend coverage is 98.01%. Frontend statement,
function, and line coverage are 98.12%, 98.54%, and 98.05%; branch coverage is
89.18%, reflecting the repository's explicit whole-source branch threshold.
The strict MkDocs build, frontend production build, and both production Docker
image builds pass. In the recreated stack Redis returned `PONG`, RabbitMQ
reported `Ping succeeded`, the backend installed `relayna==1.4.27` and
`relayna-studio==1.4.27`, and both direct and frontend-proxied service discovery
returned three services.

## Context and Orientation

Version sources are the root and Studio backend `pyproject.toml` files plus the
Studio frontend `package.json`; lock files mirror these versions. SDK freeze
manifests live in `tests/freeze/`, backend manifests in
`studio/backend/tests/freeze/`, and the frontend manifest in
`apps/studio/src/test/production-freeze-manifest.json`. User documentation is
published by MkDocs from `docs/`.

## Plan of Work

Perform the version and freeze update mechanically, refresh lock files with the
native package tools, and add a changelog entry that covers performance, UX,
coverage, documentation, and the new freeze. Run freeze tests immediately.

Use the production Docker stack at `http://localhost:3000` for screenshots.
Capture stable views with mock data, avoid recording secrets, and use a blank or
non-submitted service editor for registration/configuration examples. Expand
the Studio guide around the actual visible UI, with numbered registration steps
and tables for navigation, filters, buttons, status chips, editor fields, and
destructive actions.

Run the repository verification skill, frontend and backend coverage gates,
frontend build, MkDocs strict build, Docker builds, and a final browser smoke.
Then stage only this task's intended branch changes, commit, push, update PR
#107 with final results, mark it ready, and poll for the first review feedback.

## Validation and Acceptance

All version sources and current release documentation report `1.4.27`; all
strict freeze tests assert `v1.4.27`; historical changelog and plan records stay
historical. SDK, backend, and frontend tests pass completely, backend and
frontend executable-code coverage remain at least 98%, docs build strictly,
Docker images build, and every documentation image renders from the repository.

The PR must be ready for review and its description must include exact test,
coverage, build, docs, and live-environment results. The first actionable review
cycle must be inspected and addressed before final handoff, or reported as not
yet present after a reasonable monitoring window.

## Idempotence and Recovery

Package lock refreshes, tests, builds, screenshots, and documentation builds are
safe to rerun. Registration screenshots do not submit the form, so browser
capture is non-mutating. Generated coverage and site output are removed before
commit.
