# Security Exceptions

Relayna CI fails on high or critical dependency, filesystem, image, secret, and
static-analysis findings unless an exception is explicitly documented.

Document every entry added to `.trivyignore`, `.gitleaks.toml`, or
`.semgrep.yml` here with:

- finding ID or rule name
- owner
- affected component
- reason
- tracking link
- expiration date

## Active Exceptions

None.
